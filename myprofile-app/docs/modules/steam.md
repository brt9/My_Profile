# Steam

`SteamClient` consulta presença, biblioteca, jogos recentes e conquistas, com timeout, retry e cache. A integração é desativada quando chave ou Steam ID não estão configurados.

Imagens usam header, capsule e fallback local. Uma falha Steam nunca impede a renderização da home. Chaves são exclusivas do `.env`.
